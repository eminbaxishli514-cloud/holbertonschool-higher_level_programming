#!/usr/bin/python3
#!/usr/bin/python3

# Import the add function from add_0.py (only one mention of add_0)
from add_0 import add

# Assign values to a and b in separate lines
a = 1
b = 2

# Print the sum in the requested format
print("{} + {} = {}".format(a, b, add(a, b)))
